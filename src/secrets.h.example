#pragma once

// =========================================================================
// ARQUIVO DE EXEMPLO DE SEGREDOS
// =========================================================================
//
// INSTRUÇÕES:
// 1. Copie este arquivo e renomeie a cópia para "secrets.h" na mesma pasta (src/).
// 2. No arquivo "secrets.h", substitua o valor de GOOGLE_SCRIPT_URL_TASKS pela sua URL real.
//
// O arquivo "secrets.h" já está no .gitignore, então sua URL não será enviada
// para o repositório do Git, mantendo-a segura.

const char* GOOGLE_SCRIPT_URL_TASKS = "";
const char* GOOGLE_SCRIPT_URL_CALENDAR = "";